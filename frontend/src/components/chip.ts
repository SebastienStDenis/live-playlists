// The standard chip (docs/theme.md): an outline badge in muted text, its
// weight dialed back from the badge default so it reads as metadata.
export const CHIP_CLASS = "font-normal text-muted-foreground";
